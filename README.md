# Resume API Service

Just a simple API service to return resume experience data. Nothing fancy, just needed a 
straight-forward API to fetch career details for a front-end.

I may wrap how the data is stored in something like ```sqlite3``` but for now a manifest file that
loads property variables is enough.